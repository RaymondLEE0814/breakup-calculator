# Supabase — anon key only. It is a public key guarded by RLS; the service_role
# key must never appear in this repo or in the client bundle.
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Optional. Absent -> the site still works; results just are not persisted.
